# Core ML
torch>=2.1.0
transformers>=4.45.0
accelerate>=0.34.0
peft>=0.13.0
datasets>=3.0.0
pillow>=10.0.0
qwen-vl-utils>=0.0.8

# Training / eval utilities
bitsandbytes>=0.43.0
sentencepiece>=0.2.0
protobuf>=4.25.0
tqdm>=4.66.0
pyyaml>=6.0.1
pandas>=2.1.0
numpy>=1.26.0
scikit-learn>=1.4.0

# Optional: faster eval on CPU-only smoke tests
# timm  # pulled by some vision deps if needed
